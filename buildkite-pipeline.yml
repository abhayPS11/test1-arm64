env:
  DOCKER_USERNAME: "abhay900"
  DOCKER_PASSWORD: "Abhay@1#234"
  
steps:
  - label: "Build and push multi-arch image"
    commands:
      - docker buildx create --use --name mybuilder
      - docker buildx inspect --bootstrap
      - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
      - docker buildx build --platform linux/amd64,linux/arm64 -t your-dockerhub-username/multi-arch-app:latest --push .
    agents:
      queue: arm64


